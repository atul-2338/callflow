import OverviewDashboard from "@/components/dashboard/OverviewDashboard";

export const metadata = { title: "Dashboard — CallFlow" };

export default function DashboardPage() {
  return <OverviewDashboard />;
}
