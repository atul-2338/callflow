[Unit]
Description=Nightly CallFlow SQLite backup

[Timer]
OnCalendar=*-*-* 03:15:00
# If the box was off/asleep at 03:15, run the missed job as soon as it is back,
# instead of skipping a whole night.
Persistent=true

[Install]
WantedBy=timers.target
